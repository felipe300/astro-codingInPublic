---
import { formatDate, slugify } from "@js/utils";
import { Image } from "@astrojs/image/components";

const { title, author, date, image, category } = Astro.props;
---

<header>
  <div class="container">
    <small>
      <a href={`/category/${slugify(category)}`} class="badge">
        {category}
      </a>
    </small>
    <h1 class="h2">{title}</h1>
    <p>
      by <a href={`/author/${slugify(author)}`}>{author}</a> · {
        formatDate(date)
      }
    </p>
  </div>
  <Image
    src={image}
    alt={title}
    width={400}
    height={250}
    format="webp"
    fit="cover"
    quality={80}
    aspectRatio="4:1"
    class="hero-image"
  />
</header>
